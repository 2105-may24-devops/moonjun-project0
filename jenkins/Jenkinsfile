pipeline {
	agent { label 'master' }
	stages {
		stage('Dependencies') {
			steps {
				sh '''
				python3 -m pip install --upgrade pip && pip install -r requirements.txt
				python3 -m pip install python3-venv
				python3 -m venv venv
				. venv/bin/activate
				'''
			}
		}
	}
}