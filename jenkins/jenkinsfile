pipeline {
	agent { label 'master' }
	stages {
		stage('Dependencies') {
			steps {
				sh '''
				sudo apt-get install python3
				python3 -m pip install --upgrade pip && pip install -r requirements.txt
				'''
			}
		}
	}
}